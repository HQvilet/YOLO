version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image:  yolo-backend:latest
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: development
      PORT: 5000
    restart: unless-stopped